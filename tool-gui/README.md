# 图形化运行时环境助手

该目录包含基于 Vue 3 + Tauri 构建的运行时诊断与镜像切换工具，实现统一的玻璃拟态界面、主题同步以及离线密钥校验逻辑。主要特性：

- `src/`：包含主题样式、状态管理、窗口控制与主要页面布局
- `src-tauri/`：实现离线密钥校验、主题持久化、Python 环境模拟接口等后端逻辑
- `styles/theme.css`：定义浅色/深色主题下的整体视觉风格

> 本目录仅提供参考实现，不会影响现有 `rustscan` CLI，可独立在桌面端构建。
